{
  "name": "hhr-clone-lex-invoke",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "main": "index.mjs",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "bundle": "rm -rf ../dist-lambda && mkdir -p ../dist-lambda && cp index.mjs package.json ../dist-lambda/ && cd ../dist-lambda && npm install --omit=dev --no-package-lock && zip -qr ../dist-lambda.zip .",
    "deploy": "npm run bundle && aws lambda update-function-code --function-name hhr-clone-lex-invoke --zip-file fileb://../dist-lambda.zip"
  },
  "dependencies": {
    "@aws-sdk/client-lex-runtime-v2": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0"
  }
}
